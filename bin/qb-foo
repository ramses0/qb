if [ "--usage" == "$1" ] ; then
    echo "Foo-related commands"
    exit 2
fi
if [ "--help" == "$1" ] ; then
    echo "--lots of complicated help for qb-foo"
    echo "--help goes here of more complicated"
    exit 2
fi

#echo 'FOO=1234'
#echo 'BAR=999'
