if [ "--usage" == "$1" ] ; then
    echo "Seth related commands"
    exit 2
fi
if [ "--help" == "$1" ] ; then
    echo "--lots of complicated"
    echo "--help goes here of more complicated"
    exit 2
fi
