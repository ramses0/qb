if [ "--usage" == "$1" ] ; then
    echo "Second Tool"
    exit 2
fi
if [ "--help" == "$1" ] ; then
    echo "--lots of complicated"
    echo "--help goes here of more complicated"
    exit 2
fi

echo "blah too, the 2nd"
echo GOODBYE WORLD
